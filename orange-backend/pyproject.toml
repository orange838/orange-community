[project]
name = "orange-community"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "httpx",
    "pywrangler",
    "werkzeug",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]